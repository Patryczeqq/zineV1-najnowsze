const dotenv = require("dotenv").config({path: __dirname + "/./../../.env"})

// Zmienne

module.exports = {
    botToken: process.env.botToken,
    ownerID: "744935304271626258",
    wersjaBota: "0.1.3",
    defPref: "-"
}